@model IEnumerable<GymPrimerParcialWeb.tipo_pago>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_tipo_pag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo_pag)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_tipo_pag)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_pag)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tipo_pag }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tipo_pag }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tipo_pag })
        </td>
    </tr>
}

</table>
