@model IEnumerable<GymPrimerParcialWeb.tarjeta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.titular)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_ven)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario.nombre_usu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.titular)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_ven)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario.nombre_usu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.num_tar }) |
            @Html.ActionLink("Details", "Details", new { id=item.num_tar }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.num_tar })
        </td>
    </tr>
}

</table>
